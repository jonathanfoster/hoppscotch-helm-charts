{{- if and (eq .Values.deploymentMode "aio") .Values.aio.pdb.create }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "hoppscotch.fullname" . }}
  namespace: {{ include "hoppscotch.namespace" . | quote }}
  labels:
    app.kubernetes.io/component: hoppscotch
    {{- include "hoppscotch.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.aio.pdb.minAvailable }}
  minAvailable: {{ .Values.aio.pdb.minAvailable }}
  {{- end  }}
  {{- if or .Values.aio.pdb.maxUnavailable ( not .Values.aio.pdb.minAvailable ) }}
  maxUnavailable: {{ .Values.aio.pdb.maxUnavailable | default 1 }}
  {{- end  }}
  selector:
    matchLabels:
      app.kubernetes.io/component: hoppscotch
      {{- include "hoppscotch.selectorLabels" . | nindent 6 }}
{{- end }}
