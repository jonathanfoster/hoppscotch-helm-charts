{{- if and (eq .Values.deploymentMode "aio") .Values.aio.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "hoppscotch.fullname" . }}
  namespace: {{ include "hoppscotch.namespace" . | quote }}
  labels:
    app.kubernetes.io/component: hoppscotch
    {{- include "hoppscotch.labels" . | nindent 4 }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- toYaml .Values.commonAnnotations | nindent 4 }}
  {{- end }}
spec:
  policyTypes:
    - Ingress
    - Egress
  podSelector:
    matchLabels:
      {{- include "hoppscotch.selectorLabels" . | nindent 6 }}
  egress:
    {{- if .Values.aio.networkPolicy.allowExternalEgress }}
    - {}
    {{- else }}
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    {{- if .Values.aio.networkPolicy.extraEgress }}
    {{- toYaml .Values.aio.networkPolicy.extraEgress | nindent 4 }}
    {{- end }}
    {{- end }}
  ingress:
    - ports:
        - port: {{ .Values.aio.containerPorts.http }}
        - port: {{ .Values.aio.containerPorts.https }}
      {{- if not .Values.aio.networkPolicy.allowExternal }}
      from:
        - podSelector:
            matchLabels:
              {{- include "hoppscotch.selectorLabels" . | nindent 14 }}
        {{- if .Values.aio.networkPolicy.addExternalClientAccess }}
        - podSelector:
            matchLabels:
              {{ include "hoppscotch.fullname" . }}-client: "true"
        {{- end }}
        {{- if .Values.aio.networkPolicy.ingressPodMatchLabels }}
        - podSelector:
            matchLabels:
              {{- .Values.aio.networkPolicy.ingressPodMatchLabels | nindent 14 }}
        {{- end }}
        {{- if .Values.aio.networkPolicy.ingressNSMatchLabels }}
        - namespaceSelector:
            matchLabels:
              {{- .Values.aio.networkPolicy.ingressNSMatchLabels | nindent 14 }}
          {{- if .Values.aio.networkPolicy.ingressNSPodMatchLabels }}
          podSelector:
            matchLabels:
              {{- .Values.aio.networkPolicy.ingressNSPodMatchLabels | nindent 14 }}
          {{- end }}
        {{- end }}
      {{- end }}
    {{- if .Values.aio.networkPolicy.extraIngress }}
    {{- toYaml .Values.aio.networkPolicy.extraIngress | nindent 4 }}
    {{- end }}
{{- end }}
