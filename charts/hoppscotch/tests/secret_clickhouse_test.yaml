suite: Secret ClickHouse
templates:
  - templates/config/configmap.yaml
  - templates/config/secret.yaml
tests:
  - it: should include ClickHouse configuration when subchart is enabled and audit logs are enabled
    set:
      hoppscotch:
        backend:
          clickhouse:
            allowAuditLogs: true
      clickhouse:
        enabled: true
        auth:
          username: test-user
          password: test-password
    asserts:
      - equal:
          path: data.ALLOW_AUDIT_LOGS
          value: "true"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_HOST
          value: "RELEASE-NAME-clickhouse.NAMESPACE.svc.cluster.local:8123"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_USER
          value: dGVzdC11c2Vy
        template: templates/config/secret.yaml
      - equal:
          path: data.CLICKHOUSE_PASSWORD
          value: dGVzdC1wYXNzd29yZA==
        template: templates/config/secret.yaml

  - it: should include external ClickHouse configuration when external host is set
    set:
      hoppscotch:
        backend:
          clickhouse:
            allowAuditLogs: true
      clickhouse:
        enabled: false
      externalClickhouse:
        host: external.clickhouse.com
        port: 9123
        user: external-user
        password: external-password
    asserts:
      - equal:
          path: data.ALLOW_AUDIT_LOGS
          value: "true"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_HOST
          value: "external.clickhouse.com:9123"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_USER
          value: ZXh0ZXJuYWwtdXNlcg==
        template: templates/config/secret.yaml
      - equal:
          path: data.CLICKHOUSE_PASSWORD
          value: ZXh0ZXJuYWwtcGFzc3dvcmQ=
        template: templates/config/secret.yaml

  - it: should use custom ClickHouse configuration when provided
    set:
      hoppscotch:
        backend:
          clickhouse:
            allowAuditLogs: true
      externalClickhouse:
        host: custom.clickhouse.com
        port: 8443
        user: custom-user
        password: custom-password
    asserts:
      - equal:
          path: data.ALLOW_AUDIT_LOGS
          value: "true"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_HOST
          value: "custom.clickhouse.com:8443"
        template: templates/config/configmap.yaml
      - equal:
          path: data.CLICKHOUSE_USER
          value: Y3VzdG9tLXVzZXI=
        template: templates/config/secret.yaml
      - equal:
          path: data.CLICKHOUSE_PASSWORD
          value: Y3VzdG9tLXBhc3N3b3Jk
        template: templates/config/secret.yaml
